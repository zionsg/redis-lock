{
  "author": "Rakesh Pai <rakeshpai@errorception.com> (http://errorception.com/)",
  "homepage": "https://github.com/errorception/redis-lock",
  "name": "redis-lock",
  "description": "A locking primitive using redis.",
  "keywords": [
    "locks",
    "concurrency",
    "redis",
    "distributed"
  ],
  "version": "2.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/zionsg/redis-lock.git"
  },
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "release": "echo \"Bumping package lock and docs to version $npm_package_version\" && npm install --package-lock-only --ignore-scripts",
    "test": "node --test test/test.js"
  },
  "peerDependenciesMeta": {
    "redis": {
      "optional": true
    }
  },
  "devDependencies": {
    "redis": "^5.12.1"
  }
}
